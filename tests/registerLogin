import { assert } from 'node:console';

const { test } = require('@playwright/test');
const { RegisterLoginPage } = require('../pages/registerLogin.page');

test.describe('Register Tests', () => {
    const registerLogin = new RegisterLoginPage(page);

    test('Register a new user', async({page}) =>{

        registerLogin.registerNewUser();
        registerLogin.assertNewUser();

    }) 

    test('Login and Logout with the new user created', async({page}) {
        
        registerLogin.loginWithNewUser();
        registerLogin.assertLogIn();

        registerLogin.logoutUser();
        registerLogin.assertLogOut();


    })
})